app:
  mode: 'production'

quarkus:
  datasource:
    movies:
      db-kind: pg
      jdbc:
        url: jdbc:postgresql://localhost:5432/movies
      username: postgres
      password: postgres
  hibernate-orm:
    movies:
      datasource: movies
      dialect: org.hibernate.dialect.PostgreSQLDialect
      packages: hrytsenko
